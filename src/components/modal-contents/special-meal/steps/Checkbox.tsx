import { OptionType } from "@/constants/types/meal";

const CheckType = ({ option }: { option: OptionType }) => {
  return;
  // (
  //   <li className="flex gap-2 items-center mt-1 font-bold text-text text-3xl">
  //     <input
  //       className="w-7 h-7"
  //       type="checkbox"
  //       name={option.name}
  //       id={option.id}
  //       // checked={selectedOptions.includes(option.id)}
  //       // onChange={() =>
  //       //   setSelectedOptions((prev) => [...prev, option.id])
  //       // }
  //     />
  //     <label
  //       htmlFor={option.id}
  //       className="select-none flex items-center gap-3"
  //     >
  //       {option.img && <img src={option.img} alt="" className="w-12 h-8 " />}
  //       {option.name}
  //     </label>
  //   </li>
  // );
};

export default CheckType;
